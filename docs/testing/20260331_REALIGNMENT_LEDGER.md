# 2026-03-31 Realignment Ledger

## Status Key

- `KEEP`: preserve as-is conceptually, though implementation may still be cleaned up
- `SELECTIVE_PORT`: port only reviewed, requirement-backed hunks
- `REWRITE`: preserve the requirement, but re-implement on the clean baseline
- `DEFAULT_REVERT`: remove unless a reproduced bug or gate proves the change is needed
- `EXCLUDE`: do not treat as implementation source
- `REFERENCE_ONLY`: keep as historical context only

## Ledger

| ID | Status | Area | Current Source Candidates | Policy | Why | Closure Gate |
|---|---|---|---|---|---|---|
| LEDGER-001 | REWRITE | Native Windows lifecycle engine | `meshservice/stealth_installer.c`, `meshservice/ServiceMain.c`, `meshcore/agentcore.c`, plus current worktree edits | preserve the product requirement but rebuild around one authoritative lifecycle engine on top of `origin/main` | install/update/uninstall behavior is in scope, but current logic is spread across broad drift and duplicate entry paths | clean implementation branch has one lifecycle engine and full regression passes |
| LEDGER-002 | SELECTIVE_PORT | UMH companion lifecycle and operator path | `modules/RecoveryCore.js`, `meshcore/agentcore.c`, `meshservice/stealth_installer.c`, `modules/service-manager.js`, `test_umhctl_e2e.js`, `test/run_umh_type4_matrix.js` | keep `MasterService.exe` lifecycle, control-pipe validation, and important operator surfaces; port only the requirement-backed subset | UMH is explicitly required, but the current branch also expanded non-essential console/runtime surfaces | retained UMH surface is documented, minimal, and fully validated |
| LEDGER-003 | KEEP | Remote deployment tooling | `deploy.py`, `tools/http_connect_tunnel.py`, related deployment docs | retain `deploy.py` as the authoritative remote agent deployment tool and harden it only where needed | user explicitly wants to keep deployment tooling and it is operationally important | `deploy.py` stage/deploy/health/rollback pass and remain documented |
| LEDGER-004 | SELECTIVE_PORT | Provisioning, package staging, NodeID preservation | `meshservice/stealth_installer.c`, `meshcore/agentcore.c`, `meshservice/ServiceMain.c` | keep provisioning parity, sidecar staging, package preflight, and identity preservation logic that is explicitly tied to release gates | these behaviors are core requirements and some current fixes appear valuable, but they must be re-landed on a clean baseline | `.msh`/`.conf` parity, NodeID preservation, and package validation all pass |
| LEDGER-005 | SELECTIVE_PORT | GUI/package preflight and harness hardening | `meshservice/ServiceMain.c`, `test/gui_button_race_harness/Program.cs`, package validation paths | keep valid package-preflight and dialog-capture work, but only in the minimal form required to guarantee operator outcomes | operator-facing robustness is required, but must not drag unrelated runtime drift into core agent logic | GUI/package validation rows in the regression matrix pass with complete artifacts |
| LEDGER-006 | SELECTIVE_PORT | `RecoveryCore.js` and MeshCentral UMH operator surfacing | `modules/RecoveryCore.js` and linked operator/test paths | retain only UMH and required operator/control-pipe functionality; remove unrelated expansion, redundant console behavior, and broad drift | current file changed heavily and is a high-risk drift accumulator | retained console/operator contract is documented and Playwright-tested |
| LEDGER-007 | REWRITE | Core agent localhost-relay connectivity contract and related control-channel surface | `meshcore/agentcore.c`, related networking hunks | preserve only the explicit `agent -> localhost relay -> configured server endpoint` requirement and re-implement the minimum transport surface on the clean baseline; revert unrelated networking drift | user clarified that MeshAgent connectivity must be relayed through `localhost`, but broad proxy/control-channel drift is still not justified as-is | clean implementation branch contains only the required relay transport and explicit validation of upstream identity/trust |
| LEDGER-008 | REWRITE | Remote-desktop Session 1 `rundll32` bridge, KVM readiness path, and session-spawn surface | `meshcore/KVM/Windows/*`, `microstack/ILibProcessPipe.c`, `meshservice/stealth_watchdog.c`, related service/session-spawn changes | replace current self-exe spawning (`agent->exePath` + `-kvm0`/`-kvm1`) with `rundll32.exe` loading the KVM payload DLL (`MeshService-2022.dll` from `StealthLab_DLL` build) via `CreateProcessAsUser` in user sessions; consolidate dual token strategies into a single SYSTEM-IL approach for capture/input capability; apply DACL protection and per-helper job-object lifetime management; retire RAMAS fallback in favor of one configured rundll32 policy decision with explicit audit fields; add session change event monitoring, multi-session support, and bounded recovery on the same approved path | remote desktop continuity requires a Session 1 helper for screen capture and input injection; rundll32 is preferred over self-exe because it is Microsoft-signed, AppLocker/WDAC-allowlisted, and avoids AV/EDR flagging; the existing DLL build target (`StealthLab_DLL`) produces the payload | rundll32 bridge spawns in user sessions, captures desktop, injects input into elevated windows, handles secure desktop transitions, rejects alternate spawn paths fail-closed, and the major-bug profile passes |
| LEDGER-009 | DEFAULT_REVERT | Helper-monitor and broad user-session spawn infrastructure | `meshservice/stealth_watchdog.c`, `microstack/ILibProcessPipe.c`, policy env handling | keep default strict service-only policy; do not preserve persistent helper spawning unless explicitly required | broad session-spawn infrastructure is high-risk drift and must not silently survive | any retained user-session spawn path is explicit, minimal, logged, and validated |
| LEDGER-010 | EXCLUDE | Generated binaries and build artifacts | tracked `.dll`, `.iobj`, `.ipdb`, `.tlog`, embedded payload outputs | never use tracked build outputs as implementation truth or patch source | industrial process requires source-based reasoning and reproducible builds | no implementation decision cites build artifacts as source truth |
| LEDGER-011 | REFERENCE_ONLY | Historical audit docs and evidence sets | `20260324_*` docs, older plan docs, historical evidence | retain as historical context only | they remain useful, but they do not define the new implementation baseline | new program docs fully replace them for forward direction |
| LEDGER-012 | SELECTIVE_PORT | Current dirty worktree changes | current modified files in the working tree | no blind merge; port only individually reviewed hunks that match program requirements | current worktree contains both promising fixes and unmanaged drift | every retained hunk is traced to a TODO, a gate, and a source rationale |
| LEDGER-013 | REWRITE | Protected-screen capture sequencing | UMH operator paths, capture/injection orchestration, and supporting native timing/state code | keep the product requirement to capture before screen protections apply, but re-implement it as a minimal explicit sequencing contract | this is an end-to-end requirement, but the current broad drift does not justify carrying forward unmanaged timing and operator complexity wholesale | protected-screen flows prove ordered capture-before-protection behavior in the regression matrix |
| LEDGER-014 | REWRITE | DXGI/WGC/IDD screen capture backend | `meshcore/KVM/Windows/tile.cpp`, `meshcore/KVM/Windows/kvm.c`, open-source reference (Sunshine, OBS, RustDesk, Citrix IDD) | add DXGI Desktop Duplication and WGC backends alongside existing GDI path; GDI remains the fallback; IDD (Indirect Display Driver) is the only path that bypasses `SetWindowDisplayAffinity` | GDI misses DWM-composited overlays, hardware cursors, DX surfaces; DXGI captures these but respects display affinity (protected windows render black by design); only IDD captures below the affinity layer (Citrix shipped in production 2024; attestation signing sufficient for Win10/11 client) | DXGI produces valid frames with DX overlays GDI misses; fallback chain DXGI>WGC>GDI engages cleanly; IDD captures display-affinity-protected content |
| LEDGER-015 | SELECTIVE_PORT | Input injection verification (Session 0 bridge, BlockInput, secure desktop) | `meshcore/KVM/Windows/input.c`, `meshservice/stealth_watchdog.c`, open-source reference (UltraVNC, TightVNC) | audit the existing Session 0 bridge to verify SYSTEM IL retention through helper chain; verify `BlockInput` same-thread exemption and SYSTEM override; harden secure-desktop input path | the real obstacle is Session 0 isolation, NOT UIPI -- SYSTEM (IL=0x4000) is the highest integrity level and can `SendInput` to any window; `BlockInput(TRUE)` blocks physical input but the calling thread is exempt and SYSTEM `BlockInput(FALSE)` overrides application blocks; secure-desktop input requires explicit Winlogon desktop targeting | SYSTEM-IL helper delivers input to elevated `cmd.exe`; `SendInput` works from blocking thread; SYSTEM override of application `BlockInput(TRUE)` verified; input on Winlogon desktop during UAC |
| LEDGER-016 | SELECTIVE_PORT | Elevated-control posture (PPL awareness, early-boot, credential provider research) | `meshservice/stealth_monitor.c`, `meshservice/ServiceMain.c`, open-source reference (osquery, Windows Credential Provider samples) | add PPL detection to diagnostics; verify early-boot service readiness; evaluate credential provider integration for authentication-event visibility | PPL processes silently reject SYSTEM-level opens, confusing tamper detection; late service start misses pre-login capture; credential provider provides a sanctioned pre-login integration point | PPL status is reported in diagnostic output without false tamper alarms; service captures the Winlogon desktop immediately on boot; credential provider research spike is documented |

| LEDGER-017 | REWRITE | Undeniable network connectivity (WFP hard-permit, firewall auto-remediation, DNS resilience, explicit proxy policy) | `meshservice/stealth_firewall.c`, `meshservice/stealth_monitor.c`, `modules/win-firewall.js`, `microstack/ILibParsers.c`, open-source reference (EDRSilencer/EDRNoiseMaker WFP pattern, simplewall, Tailscale WFP sublayer weight 0xFFFF, Huntress/Datto auto-remediation, Microsoft WFP samples) | implement WFP persistent hard-permit filter at sublayer weight 0xFFFF via userland `fwpuclnt.lib` API (no kernel driver); add firewall rule auto-remediation to watchdog loop; add WFP block-filter detection and removal (EDRNoiseMaker pattern); set `SERVICE_SID_TYPE_UNRESTRICTED` for per-service firewall rules; add NRPT DNS policy override and DoH fallback for DNS blocking; retain only explicit `WebProxy=` configuration and reject ambient proxy discovery; add TLS dual-validation (strict pin first, Windows store fallback) | research confirmed: WFP operates below Windows Firewall (which uses sublayer weight 0x1000); a hard-permit at 0xFFFF overrides all standard firewall blocks; persistent filters survive reboots via BFE registry; loopback traffic is firewall-immune by default (validates localhost relay architecture); no commercial RMM uses kernel WFP drivers for connectivity -- all use userland mechanisms; Tailscale uses identical 0xFFFF sublayer pattern in production | WFP hard-permit filter installed at install time; survives firewall rule deletion, hostile block rules, and reboots; firewall auto-remediation detects and repairs within 10s; DNS resolution works through documented DNS resilience; proxy use is explicit-only through `WebProxy=`; TLS inspection survived via dual-validation |

| LEDGER-018 | REWRITE | GDI resource leaks in KVM capture and input hot paths | `meshcore/KVM/Windows/input.c`, `meshcore/KVM/Windows/tile.cpp` | fix all `GetIconInfo` calls to free returned `hbmMask` and `hbmColor` bitmaps with `DeleteObject`; fix `ReleaseDC` misuse on `CreateCompatibleDC`-created DCs (must use `DeleteDC`); fix `OpenThread(THREAD_ALL_ACCESS)` to use minimum required `THREAD_SET_CONTEXT`; fix stale handle after `CloseHandle(CUR_WORKTHREAD)` when wait times out; fix `initialize_gdiplus` to clean up predecessor handles on failure | `GetIconInfo` returns two HBITMAP handles that the caller must free; both `input.c:152` and `tile.cpp:626` leaked these on every cursor change and every frame capture; over long RMM sessions this exhausts the 10,000 GDI handle limit causing system-wide rendering failures; `ReleaseDC` was used instead of `DeleteDC` for `CreateCompatibleDC`-created DCs; `THREAD_ALL_ACCESS` violated least privilege when only `THREAD_SET_CONTEXT` was needed | GDI handle count remains stable during long KVM sessions; no leaked bitmaps, DCs, or thread handles |
| LEDGER-019 | REWRITE | KVM protocol safety (integer overflow, partial data loss, stale handles) | `meshcore/KVM/Windows/kvm.c` | fix jumbo packet size parsed as `(int)ntohl()` which overflows for malicious values > 0x7FFFFFFF (use `unsigned int` with max size validation); implement the missing partial-data `memmove` in `kvm_mainloopinput_ex` where leftover bytes after incomplete parse were silently dropped; null out stale `kvmthread` handle after `CloseHandle` | `(int)ntohl()` cast on a `uint32_t` can produce negative values that bypass size checks, enabling potential buffer overread from a malicious KVM peer; the `TODO: else move the reminder` comment confirmed partial data handling was never implemented, causing silent data loss and protocol desync; `kvmthread` held an invalid handle value after `CloseHandle` | jumbo packets with malicious sizes are rejected; partial KVM input data is correctly preserved across reads; no stale thread handles |
| LEDGER-020 | REWRITE | RecoveryCore.js security hardening (download integrity, memory safety, input validation) | `modules/RecoveryCore.js` | reject plaintext `http://` URLs for binary downloads (require `https://` only); remove `--insecure` flag that allowed remote operators to disable all TLS verification; add SHA-384 content hash verification for downloaded binaries when `--pin` is provided; replace deprecated `new Buffer(2)` with `Buffer.alloc(2)`; fix pipe response size check to validate before concatenation (not after); add periodic pruning of `umhctlFlowContextBySession` to prevent unbounded memory growth; add recursion depth limit to `umhctlEnsureDirectoryPath`; fix lifecycle guard race by checking `umhctlLifecycleOp` lock token first; replace `for...in` with `Object.keys` in `umhctlCloneObject` and `countObjectKeys` to prevent prototype pollution; add `client.removeAllListeners()` cleanup in pipe finish; use strict numeric regex in `toNumberIfNumber` | `http://` downloads with `rejectUnauthorized=0` allowed MITM binary replacement for a SYSTEM-level service; `--insecure` was remotely accessible from MeshCentral console enabling RCE via compromised download; `new Buffer()` is banned industry-wide; response size check after concatenation allowed single chunks to exceed limit; flow context dictionary leaked memory indefinitely; `for...in` without `hasOwnProperty` risked prototype pollution; pipe client kept listeners after finish | binary downloads require HTTPS with optional pinned hash verification; no remotely-disablable TLS bypass; bounded memory usage; safe object cloning |
| LEDGER-021 | REWRITE | agent-installer.js security hardening (command injection, crash safety, input sanitization) | `modules/agent-installer.js` | wrap `JSON.parse` in `fullUninstall` and `fullInstall` with try/catch to prevent agent crash on malformed input; replace Windows cleanup shell command (`cmd.exe /C` with concatenated paths) with `fs.readdirSync`/`fs.unlinkSync` to eliminate command injection; sanitize `appPrefix` for awk regex on Linux cleanup path (strip non-alphanumeric); escape double quotes in MSH values before concatenation into parameter strings; add bounds and type checking to `getParameterValue`; change `EdgeTraversalPolicy` from `allow` to `block` to reduce attack surface; fix `loc` variable used outside its declaring `try` block; fix typo `peroperty` -> `property` | `dataFolder` and `appPrefix` from service registration were concatenated into `cmd.exe /C` and `awk` commands without sanitization, enabling shell metacharacter injection; `JSON.parse` without try/catch crashed the agent process on malformed input; MSH values with embedded quotes could inject arbitrary parameters; `EdgeTraversalPolicy: 'allow'` expanded attack surface via NAT-traversed connections | no shell command injection vectors in cleanup paths; agent survives malformed JSON; MSH values sanitized; firewall rules minimized |
| LEDGER-022 | REWRITE | Token and privilege least-privilege violations in process spawning | `microstack/ILibProcessPipe.c` | replace `DuplicateTokenEx(token, MAXIMUM_ALLOWED, ...)` with specific minimum required rights `TOKEN_QUERY \| TOKEN_DUPLICATE \| TOKEN_ASSIGN_PRIMARY \| TOKEN_ADJUST_SESSIONID \| TOKEN_ADJUST_DEFAULT`; the code runs under SYSTEM context but requesting `MAXIMUM_ALLOWED` violates principle of least privilege and increases risk if code paths are ever reached from less-privileged contexts | `MAXIMUM_ALLOWED` is a security anti-pattern even when running as SYSTEM; industry best practice requires requesting only the specific token rights needed for the operation (session redirect + process creation) | spawned processes use minimum required token rights; no `MAXIMUM_ALLOWED` access requests |

## Immediate Interpretation

- `LEDGER-001`, `LEDGER-002`, `LEDGER-003`, `LEDGER-004`, and `LEDGER-005` are the required retained value.
- `LEDGER-006` is in-scope but must be reduced.
- `LEDGER-007`, `LEDGER-008`, and `LEDGER-009` are the primary networking/session drift control points; `LEDGER-007` preserves only the explicit `localhost` relay requirement and `LEDGER-008` preserves only the remote-desktop-only Session 1 bridge.
- `LEDGER-010` and `LEDGER-011` define what may not become implementation truth.
- `LEDGER-012` blocks unmanaged carry-forward from the current dirty worktree.
- `LEDGER-013` makes capture-before-protection sequencing an explicit requirement instead of an implied side effect.
- `LEDGER-014`, `LEDGER-015`, and `LEDGER-016` define the screen-protection bypass, input-blocking bypass, and elevated-control research roadmap. They are scoped by the companion roadmap document `docs/testing/20260331_SCREEN_INPUT_ELEVATED_CONTROL_ROADMAP.md`.
- `LEDGER-018` through `LEDGER-022` are the deep code audit findings implemented on 2026-03-31: GDI handle leaks, KVM protocol safety, RecoveryCore.js security hardening, agent-installer.js command injection elimination, and token least-privilege enforcement.

## Review Rule

No code port is accepted until the port note records:

- the ledger ID
- the source commit or source file
- the target requirement
- the matching regression-matrix gate
