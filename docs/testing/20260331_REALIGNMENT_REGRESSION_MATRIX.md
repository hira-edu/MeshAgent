# 2026-03-31 Realignment Regression Matrix

## Coverage Legend

- `REQUIRED`: must run before release
- `MAJORBUG_ONLY`: run when the major-bug profile is required by the gate
- `LIVE_DEPLOY`: must run for deployment qualification
- `ON_CHANGE`: required whenever the named area changes

## Matrix

| Area | Required Gate | Profile | Tooling | Evidence Target | Release Blocker |
|---|---|---|---|---|---|
| Clean install | `-fullinstall` returns `0` on a clean machine and `-validate-install` passes | REQUIRED | native CLI | `docs/testing/evidence/advanced/<ts>_clean_install/` | yes |
| Silent install | silent/non-interactive install uses the same native path and yields the same system state as interactive install | REQUIRED | native CLI plus validation | `docs/testing/evidence/advanced/<ts>_silent_install/` | yes |
| PowerShell-invoked native install | PowerShell entry invokes the native installer path only and completes successfully | REQUIRED | operator invocation plus validation | `docs/testing/evidence/advanced/<ts>_powershell_native_install/` | yes |
| Repair install | install over partial or broken state converges to a healthy installed state | REQUIRED | native CLI | `docs/testing/evidence/advanced/<ts>_repair_install/` | yes |
| Clean update | `-fullupdate` returns `0`, preserves NodeID, and `-validate-update` passes | REQUIRED | native CLI | `docs/testing/evidence/advanced/<ts>_clean_update/` | yes |
| Update over identical version | update on an already-current install is idempotent and healthy | REQUIRED | native CLI | `docs/testing/evidence/advanced/<ts>_idempotent_update/` | yes |
| Update over partial state | update succeeds from stale service registration, stale files, or partial sidecars | REQUIRED | native CLI | `docs/testing/evidence/advanced/<ts>_partial_state_update/` | yes |
| Self-update activation | live self-update path triggers native update flow and converges cleanly | ON_CHANGE | self-update harness plus validation | `docs/testing/evidence/advanced/<ts>_selfupdate_activation/` | yes |
| Clean uninstall | `-fulluninstall` returns `0` and `-validate-uninstall` passes | REQUIRED | native CLI | `docs/testing/evidence/advanced/<ts>_clean_uninstall/` | yes |
| Reinstall loop | install -> validate -> uninstall -> validate -> reinstall -> validate -> update -> validate | REQUIRED | native CLI or JS harness | `docs/testing/evidence/advanced/<ts>_reinstall_loop/` | yes |
| `-svchost-status` | service image path, group, `ServiceDll`, hash, unload-on-stop, and running state are all correct | REQUIRED | native CLI | `docs/testing/evidence/advanced/<ts>_svchost_status/` | yes |
| Service-only enforcement | standalone execution is blocked and only managed/helper entrypoints remain allowed | REQUIRED | native CLI plus negative tests | `docs/testing/evidence/advanced/<ts>_service_only_policy/` | yes |
| Registry state | service keys, parameters, persistence, and policy keys are correct after install/update and absent after uninstall | REQUIRED | validation command | `docs/testing/evidence/advanced/<ts>_registry_validation/` | yes |
| Firewall state | required firewall rules converge after install/update and are absent after uninstall | REQUIRED | validation command | `docs/testing/evidence/advanced/<ts>_firewall_validation/` | yes |
| DACL state | service and file DACLs are correct after install/update | REQUIRED | validation command | `docs/testing/evidence/advanced/<ts>_dacl_validation/` | yes |
| WMI/task persistence | WMI and scheduled-task persistence match policy after install/update and are absent after uninstall | REQUIRED | validation command plus native inspection | `docs/testing/evidence/advanced/<ts>_persistence_validation/` | yes |
| Provisioning parity | staged `.msh` and `.conf` preserve intended endpoint, `ServerID`, and required hash material | REQUIRED | provisioning check plus validation | `docs/testing/evidence/advanced/<ts>_provisioning_parity/` | yes |
| Package preflight | invalid packages fail before system mutation; valid packages succeed regardless of non-required sidecar absence | REQUIRED | package validation plus GUI/CLI runs | `docs/testing/evidence/advanced/<ts>_package_preflight/` | yes |
| Path handling | repo paths, staged paths, paths with spaces, and paths with parentheses all behave correctly | REQUIRED | GUI and CLI harnesses | `docs/testing/evidence/advanced/<ts>_path_handling/` | yes |
| Identity preservation | NodeID and identity artifacts remain stable across update and reinstall | REQUIRED | native CLI plus datastore check | `docs/testing/evidence/advanced/<ts>_identity_preservation/` | yes |
| Restart continuity | service restart exits `0`, returns `RUNNING`, and keeps required readiness | REQUIRED | native CLI | `docs/testing/evidence/advanced/<ts>_restart_continuity/` | yes |
| Reboot continuity | install survives reboot with service running, connectivity intact, and svchost validation healthy | REQUIRED | native CLI plus reboot harness | `docs/testing/evidence/advanced/<ts>_reboot_continuity/` | yes |
| Agent connectivity | service connects through the required `localhost` relay to the configured staged server endpoint and remains connected across restart and update | REQUIRED | native regression plus log/evidence capture | `docs/testing/evidence/advanced/<ts>_connectivity/` | yes |
| Localhost relay integrity | `localhost` relay evidence proves the local hop, the intended upstream endpoint identity, and the preserved trust/hash contract without silent retargeting | REQUIRED | native probe plus connectivity evidence capture | `docs/testing/evidence/advanced/<ts>_localhost_relay_integrity/` | yes |
| Unauthorized session spawn absence | no silent or policy-violating user-session spawn occurs during normal install/update/uninstall/runtime flows | REQUIRED | policy audit plus event/log capture | `docs/testing/evidence/advanced/<ts>_session_spawn_audit/` | yes |
| Remote-desktop Session 1 bridge policy | any Session 1 `rundll32` bridge is exercised only for remote desktop/KVM readiness; PowerShell, terminal, file operations, and arbitrary processes remain blocked from generic Session 1 dispatch | REQUIRED | native policy probe plus major-bug evidence capture | `docs/testing/evidence/advanced/<ts>_session1_bridge_policy/` | yes |
| Remote desktop continuity | remote desktop remains available through restart and update and major-bug self-test passes | MAJORBUG_ONLY | native CLI `--majorBug=1` profile | `docs/testing/evidence/advanced/<ts>_remote_desktop_majorbug/` | yes |
| Terminal/file surfaces | terminal and file operations work when investigating a major bug or regression in those areas | MAJORBUG_ONLY | targeted self-test/harness | `docs/testing/evidence/advanced/<ts>_terminal_file_majorbug/` | conditional |
| UMH install lifecycle | `umhctl install --url ...` installs `AdvancedHookService`, starts it, and reports healthy managed-binary state without coupling to native agent install/update | REQUIRED | agent console `umhctl` plus service probe | `docs/testing/evidence/advanced/<ts>_umh_install/` | yes |
| UMH update lifecycle | UMH stays healthy across native agent update and remains on the expected managed binary path after an operator-driven `umhctl install --url ...` baseline | REQUIRED | `umhctl` plus native agent update plus service probe | `docs/testing/evidence/advanced/<ts>_umh_update/` | yes |
| UMH uninstall lifecycle | `umhctl uninstall` removes `AdvancedHookService` and the managed UMH binary without requiring native agent uninstall | REQUIRED | agent console `umhctl` plus absence checks | `docs/testing/evidence/advanced/<ts>_umh_uninstall/` | yes |
| UMH control pipe | pipe connects and `status` succeeds after `umhctl` install/update; pipe is absent after `umhctl uninstall` | REQUIRED | console probe or control-pipe harness | `docs/testing/evidence/advanced/<ts>_umh_pipe/` | yes |
| UMH operation matrix | `listProcesses`, `status`, `inject`, `injectAll`, `telemetry`, `repair`, `setFlags`, `disable`, `disableAll`, `getPolicy`, `setPolicy`, `getConfig`, `setConfig`, `lockdownBypass`, and `examsoftBypass` all complete through the supported path | REQUIRED | UMH matrix harness | `docs/testing/evidence/advanced/<ts>_umh_operation_matrix/` | yes |
| UMH injection-profile matrix | all 20 supported injection-profile values are explicitly requested, honored, and evidenced | REQUIRED | UMH matrix harness | `docs/testing/evidence/advanced/<ts>_umh_profile_matrix/` | yes |
| Protected-screen pre-protection capture | supported protected-screen flows produce the required capture artifact before screen protections or blackout behavior are applied, or fail explicitly before protection state changes | REQUIRED | native probe plus UMH/operator harness | `docs/testing/evidence/advanced/<ts>_pre_protection_capture/` | yes |
| UMH operator desktop UI | MeshCentral desktop device page builds and dispatches UMH commands correctly | REQUIRED | Playwright | `docs/testing/evidence/advanced/<ts>_playwright_umh_desktop/` | yes |
| UMH operator mobile UI | MeshCentral mobile device page preserves the same UMH command semantics | REQUIRED | Playwright | `docs/testing/evidence/advanced/<ts>_playwright_umh_mobile/` | yes |
| GUI install/update/uninstall | GUI operator flows succeed across installed/not-installed/update/uninstall states | REQUIRED | GUI harness | `docs/testing/evidence/advanced/<ts>_gui_lifecycle/` | yes |
| GUI dialog capture | unexpected modal dialogs are captured and fail the scenario | REQUIRED | GUI harness | `docs/testing/evidence/advanced/<ts>_gui_dialog_capture/` | yes |
| Grouped-run artifact integrity | grouped harness runs always emit `summary.txt` or an explicit fatal artifact | REQUIRED | GUI and grouped harnesses | `docs/testing/evidence/advanced/<ts>_grouped_artifact_integrity/` | yes |
| Stress churn | repeated install/update/uninstall/restart loops do not leak state or strand services | REQUIRED | stress harness | `docs/testing/evidence/advanced/<ts>_stress_churn/` | yes |
| Rollback safety | failed update/install rollback leaves a healthy prior state or explicit fatal failure with evidence | REQUIRED | fault-injection harness | `docs/testing/evidence/advanced/<ts>_rollback_safety/` | yes |
| Deployment stage/deploy/health | retained `deploy.py` workflow still stages, deploys, and verifies agent binaries remotely | LIVE_DEPLOY | `deploy.py` | `docs/testing/evidence/advanced/<ts>_deploy_py_health/` | yes |
| Deployment rollback | retained `deploy.py rollback` restores the prior remote payload set cleanly | LIVE_DEPLOY | `deploy.py` | `docs/testing/evidence/advanced/<ts>_deploy_py_rollback/` | yes |
| Signer allowlist and digest gates | binaries are signed appropriately and hashes are captured for release | REQUIRED | signer validation plus digest export | `docs/testing/evidence/advanced/<ts>_release_signing/` | yes |
| Evidence-bundle export | final evidence archive is produced and ready for deployment/release records | REQUIRED | packaging step | `docs/testing/artifacts/<ts>_realignment_bundle.zip` | yes |
| DXGI capture on DWM desktop | DXGI Desktop Duplication captures a valid frame on a DWM-composited desktop including DX/GL surface overlays that GDI misses; `DuplicateOutput1` negotiates format correctly; progressive backoff recovers from `ACCESS_LOST`. Note: DXGI respects `SetWindowDisplayAffinity` by design -- protected windows render black. Only IDD bypasses display affinity. | REQUIRED | KVM harness plus DXGI backend | `docs/testing/evidence/advanced/<ts>_dxgi_capture/` | yes |
| DXGI-to-GDI fallback | capture falls back to GDI cleanly on pre-Win8, RDP session, `DXGI_ERROR_UNSUPPORTED` (hybrid GPU), or when duplication is denied | REQUIRED | KVM harness with fallback trigger | `docs/testing/evidence/advanced/<ts>_dxgi_gdi_fallback/` | yes |
| WGC capture on Win10 1903+ (cross-GPU fallback) | Windows.Graphics.Capture produces a valid per-monitor frame on Win10 1903+ when DXGI reports `DXGI_ERROR_UNSUPPORTED` on hybrid GPU. Note: WGC cannot capture secure desktop. | ON_CHANGE | KVM harness plus WGC backend | `docs/testing/evidence/advanced/<ts>_wgc_capture/` | yes |
| Secure desktop capture | frame captured from the Winlogon desktop during an active UAC prompt or lock screen via service-context `OpenDesktop("Winlogon")` with `EVENT_SYSTEM_DESKTOPSWITCH` detection | REQUIRED | KVM harness with UAC trigger | `docs/testing/evidence/advanced/<ts>_secure_desktop_capture/` | yes |
| Input injection on elevated window | keystroke delivered to an elevated `cmd.exe` from SYSTEM-IL KVM helper in user session. Verifies: helper token retains SYSTEM integrity through `CreateProcessAsUser` bridge; `SendInput` reaches high-IL window (UIPI only blocks upward, SYSTEM is highest) | REQUIRED | KVM input harness plus elevation setup | `docs/testing/evidence/advanced/<ts>_session_bridge_injection/` | yes |
| BlockInput semantics | `SendInput` succeeds from the blocking thread while `BlockInput(TRUE)` is active (same-thread exemption per Microsoft docs); SYSTEM `BlockInput(FALSE)` overrides application-level `BlockInput(TRUE)` from a separate process | REQUIRED | KVM input harness plus BlockInput trigger | `docs/testing/evidence/advanced/<ts>_blockinput_semantics/` | yes |
| Secure desktop input | mouse and keyboard input delivered on the Winlogon desktop during an active UAC prompt via `SetThreadDesktop` to Winlogon desktop from SYSTEM context | REQUIRED | KVM input harness with UAC trigger | `docs/testing/evidence/advanced/<ts>_secure_desktop_input/` | yes |
| PPL diagnostic accuracy | PPL-protected processes are reported in diagnostic output via `NtQueryInformationProcess(ProcessProtectionInformation)`; tamper-detection does not raise false alarms when `OpenProcess` fails on PPL processes | REQUIRED | diagnostic command plus PPL process presence | `docs/testing/evidence/advanced/<ts>_ppl_diagnostic/` | yes |
| GPU encoding benchmark | Sunshine-style zero-copy pipeline: shared texture handle + `IDXGIKeyedMutex` + NVENC/AMF/QSV. Documents probe results, CPU/FPS benchmark vs JPEG tiles, fallback chain behavior | ON_CHANGE | benchmark harness | `docs/testing/evidence/advanced/<ts>_gpu_encoding_benchmark/` | conditional |
| IDD display-affinity capture | Indirect Display Driver captures a frame from a `WDA_EXCLUDEFROMCAPTURE`-protected window. This is the ONLY capture path that bypasses display affinity (DXGI/WGC/GDI all respect it). Attestation-signed UMDF driver. | ON_CHANGE | IDD driver plus capture harness | `docs/testing/evidence/advanced/<ts>_idd_affinity_capture/` | conditional |
| rundll32 KVM bridge spawn | `rundll32.exe <dllpath>,KvmSessionBridgeW <pipename>` spawns in the user session via `CreateProcessAsUser` with SYSTEM token + `SetTokenInformation(TokenSessionId)`; the DLL connects to the service's named pipe within 5s; capture frames flow from DLL to service; the process runs as SYSTEM IL in the user session | REQUIRED | KVM harness with rundll32 bridge | `docs/testing/evidence/advanced/<ts>_rundll32_bridge_spawn/` | yes |
| rundll32 bridge DACL protection | spawned rundll32 process has deny-PROCESS_TERMINATE ACE for Everyone SID; standard user cannot kill the process via Task Manager; SYSTEM service retains control via existing process handle | REQUIRED | Task Manager kill attempt plus process DACL dump | `docs/testing/evidence/advanced/<ts>_rundll32_dacl_protection/` | yes |
| rundll32 bridge Job Object cleanup | rundll32 helper is assigned to Job Object with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE`; when the service stops, the rundll32 process is automatically terminated; no orphaned rundll32 processes remain | REQUIRED | service stop plus process list verification | `docs/testing/evidence/advanced/<ts>_rundll32_job_cleanup/` | yes |
| rundll32 bridge graceful shutdown | service sends shutdown command over named pipe; DLL sets shutdown event; capture/input threads exit; function returns; `DllMain(DLL_PROCESS_DETACH)` runs; rundll32 exits cleanly within 5s; service `TerminateProcess` fallback handles unresponsive DLL | REQUIRED | graceful shutdown harness with timing evidence | `docs/testing/evidence/advanced/<ts>_rundll32_graceful_shutdown/` | yes |
| rundll32 bridge RAMAS cascade | spawn attempts try `SPECIFIED_USER -> WINLOGON -> USER -> WINLOGON` with rundll32; each attempt has a 5s pipe connect timeout; logging records attempt type, session ID, and outcome; first successful connection wins | REQUIRED | RAMAS harness with multi-attempt logging | `docs/testing/evidence/advanced/<ts>_rundll32_ramas_cascade/` | yes |
| rundll32 bridge session change | on `WTS_SESSION_LOGOFF` / `WTS_CONSOLE_DISCONNECT`: current rundll32 terminated gracefully, new one spawned in newly active session; on `WTS_SESSION_LOCK`: helper terminated, respawned on unlock; no orphaned processes across session transitions | REQUIRED | session change harness with lock/unlock/logoff/switch | `docs/testing/evidence/advanced/<ts>_rundll32_session_change/` | yes |
| rundll32 bridge crash recovery | if rundll32 crashes or DLL fails to load (exit within 2s, nonzero exit code), service detects via `GetExitCodeProcess`, logs the failure, and retries with exponential backoff (2s/4s/8s/16s/32s/60s cap); self-exe fallback available if rundll32 consistently fails | REQUIRED | fault injection (missing DLL, crash simulation) | `docs/testing/evidence/advanced/<ts>_rundll32_crash_recovery/` | yes |
| rundll32 bridge input on elevated window | rundll32 helper running as SYSTEM IL in user session delivers keystroke to an elevated `cmd.exe` via `SendInput`; UIPI does not block (SYSTEM > High IL) | REQUIRED | input harness with elevated target | `docs/testing/evidence/advanced/<ts>_rundll32_elevated_input/` | yes |
| rundll32 bridge secure desktop | when active desktop switches to Winlogon (UAC/lock screen), rundll32 spawned with `Winsta0\Winlogon` desktop captures the secure desktop and injects input | REQUIRED | UAC prompt trigger plus capture/input verification | `docs/testing/evidence/advanced/<ts>_rundll32_secure_desktop/` | yes |
| rundll32 bridge named pipe audit | every spawn writes a Windows Event Log entry (PID, session ID, DLL path, token type, outcome) via `ReportEventW`; entries are visible in Event Viewer under the service name | REQUIRED | Event Log inspection after spawn cycle | `docs/testing/evidence/advanced/<ts>_rundll32_event_audit/` | yes |
| WFP hard-permit persistence | WFP persistent permit filter at sublayer weight `0xFFFF` with `FWPM_FILTER_FLAG_CLEAR_ACTION_RIGHT` survives reboot; `netsh wfp show filters` confirms filter; filter persists in BFE registry; non-admin user cannot delete it | REQUIRED | reboot plus filter enumeration | `docs/testing/evidence/advanced/<ts>_wfp_permit_persistence/` | yes |
| WFP overrides firewall block | Windows Firewall block rule created for agent exe via `netsh advfirewall firewall add rule dir=out action=block program=<exe>`; agent connectivity is NOT interrupted because WFP hard-permit at sublayer 0xFFFF overrides the firewall block at sublayer 0x1000 | REQUIRED | firewall block rule + connectivity test | `docs/testing/evidence/advanced/<ts>_wfp_override_block/` | yes |
| WFP hostile block removal | external WFP block filter added targeting agent's app ID via `FwpmFilterAdd0`; watchdog detects and removes the hostile block within 60s; connectivity restored | REQUIRED | hostile WFP filter injection + detection timing | `docs/testing/evidence/advanced/<ts>_wfp_hostile_removal/` | yes |
| Firewall rule auto-remediation | agent's Windows Firewall allow rule is deleted via `netsh advfirewall firewall delete rule`; watchdog re-creates it within 30s; hostile block rule targeting agent exe is created and removed by watchdog | REQUIRED | rule deletion + hostile rule + timing | `docs/testing/evidence/advanced/<ts>_firewall_autoremediation/` | yes |
| Service SID firewall scoping | service configured with `SERVICE_SID_TYPE_UNRESTRICTED`; firewall rules scoped to `ServiceName=WinDiagnosticHost`; bulk deletion of svchost rules does NOT affect agent-specific rules | REQUIRED | service SID verification + bulk rule delete test | `docs/testing/evidence/advanced/<ts>_service_sid_firewall/` | yes |
| DNS fallback chain | local DNS server is unreachable or returns NXDOMAIN for server; NRPT override routes to public DNS; if NRPT fails, DoH fallback resolves via Cloudflare/Google; if DoH fails, cached last-known-good IP is used; if cache empty, hardcoded IPs from network profile connect directly | REQUIRED | DNS blocking simulation + resolution verification | `docs/testing/evidence/advanced/<ts>_dns_fallback_chain/` | yes |
| TLS inspection survival | corporate TLS-inspecting proxy (simulated) intercepts agent connection and re-signs with corporate CA; strict certificate pin fails; dual-validation fallback trusts Windows system store; connection succeeds; inspection detection is logged | REQUIRED | MITM proxy simulation + connectivity test | `docs/testing/evidence/advanced/<ts>_tls_inspection_survival/` | yes |
| Proxy fallback chain | agent connects through corporate HTTP proxy from SYSTEM context; tests: (1) direct fails, (2) machine-level WinHTTP proxy used, (3) user proxy from registry, (4) WPAD auto-detection; working proxy cached in datastore | REQUIRED | proxy configuration + connectivity test chain | `docs/testing/evidence/advanced/<ts>_proxy_fallback_chain/` | yes |
| Loopback firewall immunity | Windows Firewall "block all inbound" profile is active; localhost relay connection (agent <-> local listener on 127.0.0.1) is NOT blocked; external outbound connection uses WFP hard-permit | REQUIRED | restrictive firewall profile + loopback + outbound test | `docs/testing/evidence/advanced/<ts>_loopback_immunity/` | yes |

| GDI handle stability | GDI handle count remains stable during extended KVM sessions with cursor changes and frame captures; no leaked `HBITMAP` handles from `GetIconInfo`; `CreateCompatibleDC` DCs freed with `DeleteDC` not `ReleaseDC` | REQUIRED | KVM session harness with handle counter | `docs/testing/evidence/advanced/<ts>_gdi_handle_stability/` | yes |
| KVM protocol safety | jumbo packets with sizes > 10MB or near UINT_MAX are rejected; partial KVM input data spanning multiple reads is correctly reassembled without data loss; no stale thread handles after `CloseHandle` | REQUIRED | KVM protocol harness with malformed input | `docs/testing/evidence/advanced/<ts>_kvm_protocol_safety/` | yes |
| Token least-privilege | process spawning via `CreateProcessAsUser` requests only minimum required token rights (`TOKEN_QUERY \| TOKEN_DUPLICATE \| TOKEN_ASSIGN_PRIMARY \| TOKEN_ADJUST_SESSIONID \| TOKEN_ADJUST_DEFAULT`); no `MAXIMUM_ALLOWED` access requests | REQUIRED | token rights inspection | `docs/testing/evidence/advanced/<ts>_token_least_privilege/` | yes |
| Binary download HTTPS-only | `umhctl install` rejects `http://` URLs; no `--insecure` flag available; SHA-384 content hash verified when `--pin` provided; hash mismatch aborts install with tamper warning | REQUIRED | download harness with pin verification | `docs/testing/evidence/advanced/<ts>_binary_download_security/` | yes |
| RecoveryCore memory safety | pipe response size checked before concatenation; flow context dictionary pruned every 5 minutes; directory creation recursion bounded to 20 levels; lifecycle guard prevents rapid re-entry; no deprecated `Buffer()` usage; `Object.keys` used instead of `for...in` | REQUIRED | RecoveryCore VM sandbox harness | `docs/testing/evidence/advanced/<ts>_recoverycore_memory_safety/` | yes |
| Installer input validation | `fullInstall`/`fullUninstall` survive malformed JSON without crash; Windows cleanup uses `fs` API not shell commands; MSH values sanitized (quotes stripped); `getParameterValue` bounds-checked; `EdgeTraversalPolicy` set to `block` | REQUIRED | installer harness with malformed input | `docs/testing/evidence/advanced/<ts>_installer_input_validation/` | yes |

## Execution Rules

- `REQUIRED` rows must all pass before release.
- `MAJORBUG_ONLY` rows are still release blockers where the standing requirement names the major-bug profile.
- `LIVE_DEPLOY` rows are mandatory before declaring deployment qualification complete.
- Every matrix row must have a reproducible command line or harness invocation recorded in the evidence folder.
